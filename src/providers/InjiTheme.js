import { createTheme } from '@mui/material/styles';

const injiTheme = createTheme({
    palette: {
        primary: {
            main: '#E86E04',
            light: '#F59B4B'
        },
    },
    typography: {
        fontFamily: 'Inter'
    }
});
